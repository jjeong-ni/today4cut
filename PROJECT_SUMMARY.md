# TODAY 4 CUT 프로젝트 정리

작성일: 2026-06-04  
프로젝트 위치: `C:\Users\jeong\Documents\Codex\2026-05-30\https-heyhye-photobooth-github-io-filter4`

## 1. 기획의도

기존 `heyhye-photobooth` 형태의 웹 포토부스 페이지를 분석한 뒤, 문제점을 보완해서 새 웹 포토부스 서비스로 다시 만드는 것이 목적이다.

새 서비스의 이름은 한국어 `오늘4컷` 대신 영어 브랜드명인 `TODAY 4 CUT`으로 정리했다. 사용자는 회원가입 없이도 바로 사진을 찍고 저장할 수 있어야 하며, 로그인한 사용자는 촬영한 사진이 보관함에 저장되어 다음 접속 때도 다시 확인하고 저장할 수 있어야 한다.

또한 데스크톱, 안드로이드, iOS 모바일 화면에 맞게 자동으로 반응하는 화면 구성이 필요했고, 촬영 전에 4컷 배열 방식을 선택한 뒤 선택한 배열에 맞춰 한 화면 안에서 4개의 촬영 칸이 보이도록 만드는 것이 핵심 방향이었다.

## 2. 핵심 사용자 흐름

1. 사용자가 웹페이지에 접속한다.
2. 비회원으로 바로 촬영하거나, 회원가입/로그인을 선택한다.
3. 촬영 전에 4컷 레이아웃 3가지 중 하나를 고른다.
4. 선택한 레이아웃에 맞춰 한 화면에 4개의 카메라 칸이 표시된다.
5. 1번 칸부터 4번 칸까지 순서대로 촬영한다.
6. 필터를 선택해서 촬영할 수 있다.
7. 촬영 완료 후 결과물을 미리보기로 확인한다.
8. 결과물을 다운로드하거나 공유한다.
9. 로그인 상태라면 완성된 사진이 보관함에 저장된다.

## 3. 완료된 사항

### 브랜드 및 화면 문구 정리

- 기존 원본 URL에서 유래한 `heyhye` 관련 흔적을 제거했다.
- 서비스명을 `TODAY 4 CUT`으로 통일했다.
- 한글 제목, 원본 사이트명, 임시 문구가 섞이지 않도록 주요 화면 문구를 정리했다.

### 디자인 및 반응형 화면

- 귀여운 느낌의 웹폰트 `Jua`, `Gaegu`를 적용했다.
- 모바일과 데스크톱 화면 크기에 맞춰 레이아웃이 조정되도록 CSS를 구성했다.
- 촬영 화면, 레이아웃 선택 화면, 미리보기 화면, 보관함 화면이 작은 화면에서도 사용할 수 있도록 조정했다.

### 촬영 기능

- 카메라 접근 기능을 구현했다.
- 카메라 권한 요청 실패 시 안내 메시지와 다시 시도 버튼이 나오도록 보완했다.
- `localhost` 또는 HTTPS 환경에서 카메라가 정상 작동하도록 구성했다.
- 촬영 전 4컷 구성 선택지를 3가지로 만들었다.
  - 세로형 4컷
  - 2x2 그리드형
  - 큰 컷 1개 + 작은 컷 3개 조합
- 선택한 레이아웃에 따라 촬영 화면의 4개 칸 배치가 달라진다.
- 한 화면 안에서 4개의 칸을 보고, 1번부터 4번까지 순서대로 촬영되도록 구현했다.

### 필터 및 결과물

- 기본 필터, 흑백, CCD 느낌, 소프트 필터를 적용했다.
- 촬영 완료 후 선택한 레이아웃에 맞춰 최종 이미지를 캔버스로 합성한다.
- 완성된 이미지를 다운로드할 수 있다.
- 브라우저가 지원하는 경우 공유 기능을 사용할 수 있다.

### 회원가입, 로그인, 보관함

- 비회원도 촬영, 저장, 공유가 가능하도록 했다.
- 회원가입과 로그인 기능을 추가했다.
- 로그인한 사용자의 완성 사진은 브라우저 저장소에 보관된다.
- 다음 접속 시 같은 브라우저에서 보관함을 다시 열어 사진을 확인할 수 있다.
- 보관함에서 사진 다운로드와 삭제가 가능하다.

주의: 현재 회원 기능은 서버가 없는 로컬 브라우저 저장 방식이다. 같은 브라우저/기기에서는 유지되지만, 다른 기기나 다른 브라우저로 자동 동기화되지는 않는다.

## 4. 결과물 파일 위치

### 프로젝트 폴더

`C:\Users\jeong\Documents\Codex\2026-05-30\https-heyhye-photobooth-github-io-filter4`

### 주요 파일

- 메인 HTML  
  `C:\Users\jeong\Documents\Codex\2026-05-30\https-heyhye-photobooth-github-io-filter4\index.html`

- 디자인/CSS  
  `C:\Users\jeong\Documents\Codex\2026-05-30\https-heyhye-photobooth-github-io-filter4\styles.css`

- 기능/JavaScript  
  `C:\Users\jeong\Documents\Codex\2026-05-30\https-heyhye-photobooth-github-io-filter4\app.js`

- 실행 안내 문서  
  `C:\Users\jeong\Documents\Codex\2026-05-30\https-heyhye-photobooth-github-io-filter4\README.md`

- 로컬 실행용 파일  
  `C:\Users\jeong\Documents\Codex\2026-05-30\https-heyhye-photobooth-github-io-filter4\start-today4cut.cmd`

- 배포/저장소 제외 설정  
  `C:\Users\jeong\Documents\Codex\2026-05-30\https-heyhye-photobooth-github-io-filter4\.gitignore`

- 현재 정리 문서  
  `C:\Users\jeong\Documents\Codex\2026-05-30\https-heyhye-photobooth-github-io-filter4\PROJECT_SUMMARY.md`

### 검증용 이미지와 로그

작업 중 화면 확인을 위해 아래와 같은 미리보기 이미지와 서버 로그 파일이 생성되었다.

- `preview-mobile.png`
- `preview-layout.png`
- `preview-camera.png`
- `preview-desktop.png`
- `server.log`
- `server2.log`

이 파일들은 개발 중 확인용 결과물이며, 실제 웹사이트 배포에 필수인 파일은 아니다. `.gitignore`에 포함되어 GitHub 업로드 대상에서 제외되도록 설정했다.

## 5. 현재 접속 주소

### 내 컴퓨터에서 확인하는 주소

`http://127.0.0.1:4173/index.html`

이 주소는 내 컴퓨터 안에서만 접속되는 주소다. 다른 사람에게 공유해도 상대방 컴퓨터에서는 접속되지 않는다.

### 같은 와이파이에서 임시 확인하는 주소

`http://172.20.146.83:4173/index.html`

같은 와이파이에 연결된 기기에서는 이 주소로 화면 확인이 가능할 수 있다. 다만 모바일 카메라는 일반 HTTP 주소에서 차단될 가능성이 높다.

### 외부 공유용 권장 방식

다른 사람에게 안정적으로 공유하려면 GitHub Pages 같은 HTTPS 배포가 필요하다.

예상되는 최종 공유 주소 형식:

`https://jjeong-ni.github.io/today4cut/`

실제 주소는 GitHub 저장소 이름과 GitHub Pages 설정에 따라 달라질 수 있다.

## 6. 문제 발생 부분 및 원인

### PowerShell에서 웹페이지가 제대로 안 보인 문제

PowerShell에서 서버를 실행할 때 프로젝트 폴더가 아니라 `C:\WINDOWS\system32` 위치에서 실행되었다. 그래서 브라우저가 프로젝트의 `index.html`, `styles.css`, `app.js`를 제대로 찾지 못하거나 파일 목록/글씨처럼 보이는 화면이 나올 수 있었다.

해결 방법으로 `start-today4cut.cmd` 파일을 만들어, 프로젝트 폴더 기준으로 서버가 실행되도록 했다.

### 이미지가 안 보이고 글씨만 보인 문제

서버가 잘못된 폴더에서 열리거나, 필요한 CSS/JavaScript 파일이 정상 로드되지 않으면 웹앱 화면이 깨져 보일 수 있다. 현재는 프로젝트 폴더에서 실행하면 CSS와 JavaScript가 정상 응답하는 것을 확인했다.

### 카메라 권한 문제

브라우저 카메라는 보안 정책상 보통 아래 환경에서만 허용된다.

- `localhost`
- `127.0.0.1`
- HTTPS 주소

따라서 `http://172.20.146.83:4173/index.html` 같은 일반 HTTP 와이파이 주소에서는 특히 모바일 기기에서 카메라가 막힐 수 있다. 외부 공유와 모바일 카메라 테스트를 위해서는 HTTPS 배포가 필요하다.

### GitHub Desktop Clone 오류

GitHub Desktop에서 `https://github.com/jjeong-ni/today4cut.git` 주소를 Clone하려고 했지만, 해당 저장소가 아직 없거나 접근 가능한 상태가 아니어서 오류가 발생했다.

현재 해야 할 작업은 Clone이 아니라 기존 로컬 프로젝트 폴더를 GitHub Desktop에 추가한 뒤, 새 저장소로 Publish하는 것이다.

### 외부 공유 링크 미완성

현재 로컬 주소는 외부 공유 링크가 아니다. 다른 사람이 접속할 수 있는 웹링크를 만들려면 GitHub에 저장소를 만들고 GitHub Pages를 켜야 한다.

### 회원 보관함의 한계

현재 로그인/보관함 기능은 서버 없이 브라우저 저장소를 사용하는 방식이다. 그래서 같은 브라우저에서는 보관함이 유지되지만, 다른 기기에서 같은 계정으로 로그인해도 사진이 자동으로 따라오지는 않는다.

다른 기기에서도 같은 계정의 사진을 보려면 별도의 서버, 데이터베이스, 사용자 인증, 이미지 저장소가 필요하다.

### 인앱 브라우저 자동 검증 제한

Codex 인앱 브라우저 자동화 도구가 내부 샌드박스 문제로 바로 사용할 수 없었다. 그래서 별도의 브라우저 검증 방식으로 화면과 기능을 확인했다.

## 7. 검증된 내용

- `app.js` 문법 검사가 통과했다.
- HTML에서 사용하는 주요 버튼과 화면 ID가 JavaScript에서 정상적으로 연결되는지 확인했다.
- 로컬 서버에서 아래 파일들이 정상 응답하는 것을 확인했다.
  - `index.html`
  - `styles.css`
  - `app.js`
- 가상 카메라 환경에서 촬영 흐름을 확인했다.
- 레이아웃 선택 화면에서 3가지 선택지가 표시되는 것을 확인했다.
- 촬영 화면에서 선택한 레이아웃에 따라 4개 칸이 표시되는 것을 확인했다.
- 1번부터 4번까지 순서대로 촬영 후 미리보기 화면으로 넘어가는 것을 확인했다.
- 완성 이미지 캔버스가 생성되는 것을 확인했다.

## 8. 앞으로 필요한 작업

1. GitHub Desktop에서 현재 로컬 프로젝트 폴더를 저장소로 추가한다.
2. GitHub에 `today4cut` 같은 새 저장소 이름으로 Publish한다.
3. GitHub Pages를 설정해서 HTTPS 외부 공유 링크를 만든다.
4. 배포된 HTTPS 주소에서 실제 안드로이드와 iOS 기기로 카메라 권한을 테스트한다.
5. 여러 기기에서 같은 계정 보관함을 쓰고 싶다면 서버형 로그인/저장 기능으로 확장한다.

## 9. 현재 상태 요약

현재 `TODAY 4 CUT` 웹 포토부스는 로컬에서 실행 가능한 상태다. 비회원 촬영, 회원가입/로그인, 로그인 사용자 보관함, 3가지 4컷 레이아웃 선택, 순차 촬영, 필터, 다운로드, 공유 기능까지 구현되어 있다.

남은 핵심 작업은 외부 공유용 HTTPS 배포다. 특히 모바일 카메라 권한은 HTTPS 여부가 중요하므로 GitHub Pages 배포 후 실제 기기 테스트가 필요하다.
