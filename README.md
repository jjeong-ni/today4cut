# today4cut

간단한 GitHub Pages 테스트 저장소입니다.

배포할 파일:

- `index.html`
- `styles.css`
- `app.js`

설치 및 배포

1. 레포 생성 후 `main` 브랜치에 `/root` 폴더로 Pages 배포 선택
2. 혹은 로컬에서 git 초기화 후 `gh repo create`로 리모트 생성하고 푸시하세요.
